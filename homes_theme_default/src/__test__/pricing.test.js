import Pricing from '../js/components/pricing';

test('should populate pricing select based on selected sale type being sales',() => {

});

test('should populate pricing select based on selected sale type being lettings',() => {

});

test('returns selected index of select', ()=>{

});

test('returns false when no item in select chosen', ()=>{

});

test('creates options in passed select list with no previously selected', () => {

});

test('creates options in passed select list with previously selected', () => {

});